rules_version = '2';

// Reglas de Firebase Storage para el proyecto "indunilo" (SIGTH).
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
